import 'package:flutter/material.dart';

class LoadinPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('LoadinPage'),
      ),
    );
  }
}
